name: tenamama
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  intl: ^0.20.2

  # Firebase
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.2

  # Day 2 – Local notifications
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.2


dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
